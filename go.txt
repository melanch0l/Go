package main

import (
	"fmt"
)

func main() {
	var name string = "Min"
	fmt.Printf("this is my name %s\n", name)
	var age int = 26
	fmt.Printf("this is my age %d\n", age)
	fmt.Println("Hi Now")
	fmt.Println("Nice to meet Ya")
	var num int
	var word string
	var isWorking bool
	fmt.Printf("number is : %d word is : %s and currently working : %t\n", num, word, isWorking)
	sum, _ := calculateSumAndProduct(1, 2) //_ will ignore second args
	fmt.Printf("sum number is : %d\n", sum)
	counter := 0
	for {
		if counter > 3 {
			break
		}
		fmt.Printf("the counterdown is: %d\n", counter)
		counter++
	}
	dataArray := [5]string{"min", "nyi", "nyi", "may", "zune"}
	fmt.Printf("your data array is %s\n", dataArray[0])
	newDataArray := []string{"min", "nyi", "nyi", "may", "zune"}
	newDataArray = append(newDataArray, "oo")
	fmt.Printf("your appended new data array is %s\n", newDataArray)
	newStoryArray := []string{"meet", "at", "marketplace"}
	newDataArray = append(newDataArray, newStoryArray...)
	fmt.Printf("your appended to data array with new story array is %s\n", newDataArray)
	for index, values := range newDataArray {
		fmt.Printf("Index is %d and Values is %s\n", index, values)
	}
	for _, values := range newDataArray {
		fmt.Printf("Values is %s\n", values)
	}
	homeTown := map[string][]string{
		"Palace": {"Yangon", "Lonkhin"}, //arrayofstring
		"Nation": {"Myanmar"},
	}
	fmt.Printf("Min was born in:%s\nMay Zune was born in:%s\nBoth are from:%s\n", homeTown["Palace"][1], homeTown["Palace"][0], homeTown["Nation"][0])
	nationChecker, exists := homeTown["Nation"]
	if exists {
		fmt.Println("Their Nationality is:", nationChecker[0])
	} else {
		fmt.Println("Unkown Nation")
	}
	delete(homeTown, "Nation")
	fmt.Println("Nation is delete:", homeTown)
	fmt.Printf("Nation is delete:%v", homeTown)

}
func calculateSumAndProduct(a, b int) (int, int) {
	return a + b, a * b
}

